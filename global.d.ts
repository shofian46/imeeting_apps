declare module "*.css" {
  // Deklarasi ini memungkinkan import file CSS.
  // Jika Anda menggunakan CSS Modules, Anda akan menggunakan deklarasi yang berbeda.
  // Untuk file CSS global seperti 'globals.css', ini sudah cukup.
}
